apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: Modul324_AB5_Shanthosh # Change this to your app name
  namespace: m324-2
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    acme.cert-manager.io/http01-edit-in-place: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - hosts:
        - shanthosh04.m324.hackerman.ch # Change this to your base url example: my-subdomain.m324.hackerman.ch
      secretName: shanthosh04.m324.hackerman.ch-tls # Change this to your base url example: my-subdomain.m324.hackerman.ch-tls
  rules:
    - host: shanthosh04.m324.hackerman.ch # Change this to your base url example: my-subdomain.m324.hackerman.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: Modul324_AB5_Shanthosh # Change this to your app name
                port:
                  number: 80
